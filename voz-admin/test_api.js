const { SignJWT } = require('jose'); async function run() { const secret = new TextEncoder().encode('voz-admin-dev-secret-change-in-production-2024'); const token = await new SignJWT({ employeeId: '953a7233-d2af-4907-b8a4-ed06350cbf76', username: 'admin', role: 1, workerNumber: '002' }).setProtectedHeader({ alg: 'HS256' }).setIssuedAt().setExpirationTime('8h').setIssuer('voz-admin').setAudience('voz-admin-client').sign(secret); const res = await fetch('https://voz-admin-murex.vercel.app/api/voz/users', { headers: { Authorization: 'Bearer ' + token } }); const text = await res.text(); console.log('Status:', res.status); console.log('Body:', text.substring(0, 200)); } run();
